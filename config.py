# The config is customizable to different options.
# Make sure each list has the same length!
config = {
    "initial": [10**5, 10**6],
    "avg_interest": [0.07, 0.08],
    "inflation": [0.02, 0.025],
    "monthly_contributions": [200, 100],
    "time_horizon": [40, 45]
}
